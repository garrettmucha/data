name: Test Lightning Runner
on: 
  workflow_dispatch:
  push:
    branches: [ main ]

jobs:
  test:
    runs-on: [self-hosted, lightning-fast] 
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Test runner
        run: |
          echo "🚀 Lightning Runner is working!"
          hostname
          whoami
